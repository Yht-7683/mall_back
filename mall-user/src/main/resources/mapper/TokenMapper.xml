<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yht.malluser.dao.TokenDao">

    <select id="queryByToken" resultType="com.yht.common.DO.MallUserTokenDO">
		select * from mall_user_token where token = #{value}
	</select>
</mapper>